// Currency selector sheet — UI implementation pending.
import 'package:flutter/material.dart';

/// Bottom sheet for selecting a currency from 30 supported currencies.
/// Full UI implementation pending.
class CurrencySelectorSheet extends StatelessWidget {
  const CurrencySelectorSheet({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Currency Selector')));
}
