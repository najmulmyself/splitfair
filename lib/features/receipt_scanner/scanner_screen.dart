// Receipt scanner screen — Phase 2 Pro feature. UI implementation pending.
import 'package:flutter/material.dart';

/// Receipt scanner with Apple Vision OCR (Phase 2 — Pro feature).
/// Full UI implementation pending.
class ScannerScreen extends StatelessWidget {
  const ScannerScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Scanner')));
}
