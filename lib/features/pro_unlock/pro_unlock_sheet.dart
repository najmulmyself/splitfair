// Pro unlock sheet — UI implementation pending.
import 'package:flutter/material.dart';

/// Paywall sheet for unlocking SplitFair Pro features.
/// Full UI implementation pending.
class ProUnlockSheet extends StatelessWidget {
  const ProUnlockSheet({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Pro Unlock')));
}
