// Onboarding screen — UI implementation pending.
// This file is a scaffold placeholder.
// See AMORTLY_REQUIREMENTS.md → Part 6.1 for the full implementation spec.
import 'package:flutter/material.dart';

/// Onboarding flow shown only on first launch.
/// Full UI implementation pending.
class OnboardingScreen extends StatelessWidget {
  const OnboardingScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Onboarding')));
}
