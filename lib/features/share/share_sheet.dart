// Share sheet — UI implementation pending.
import 'package:flutter/material.dart';

/// Share results sheet — captures result card as image and opens native share.
/// Full UI implementation pending.
class ShareSheet extends StatelessWidget {
  const ShareSheet({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Share')));
}
