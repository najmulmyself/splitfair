// Result card widget — the shareable image rendered via screenshot package.
import 'package:flutter/material.dart';

/// The shareable result card widget captured as a PNG via screenshot package.
/// Full UI implementation pending.
class ResultCardWidget extends StatelessWidget {
  const ResultCardWidget({super.key});

  @override
  Widget build(BuildContext context) =>
      const SizedBox.shrink();
}
