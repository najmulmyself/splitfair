// sf_bottom_sheet.dart — UI implementation pending.
import 'package:flutter/material.dart';

/// Base bottom sheet wrapper. Full implementation pending.
class SFBottomSheet extends StatelessWidget {
  const SFBottomSheet({super.key, required this.child, this.title});

  final Widget child;
  final String? title;

  @override
  Widget build(BuildContext context) => child;
}
